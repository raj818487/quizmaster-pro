# QuizMaster Pro - Production Environment
NODE_ENV=production

# Production Database (Use DATABASE_URL for cloud providers)
# Option 1: Individual database parameters
# DB_HOST=your-production-host
# DB_PORT=5432
# DB_NAME=quizmaster_prod
# DB_USER=your-prod-user
# DB_PASSWORD=your-prod-password

# Option 2: Full connection string (recommended for cloud)
DATABASE_URL=postgresql://postgres:WDjzRpZFbTSYsetmsemUYxCJbGKoWUzM@gondola.proxy.rlwy.net:45394/railway

# Production Server
PORT=4000
HOST=0.0.0.0

# Production JWT Secret (MUST be changed to a strong, unique secret)
JWT_SECRET=railway-prod-jwt-secret-key-strong-32-chars-minimum-2025
JWT_EXPIRES_IN=2h

# Password hashing (higher rounds for production security)
BCRYPT_ROUNDS=12

# CORS - Set to your actual frontend domain
CORS_ORIGIN=https://quizmaster-pro-lemon.vercel.app

# API Configuration
API_PREFIX=/api
API_VERSION=v1

# Production Logging
LOG_LEVEL=info
LOG_FILE=logs/production.log

# Rate Limiting (strict for production)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Cloud Provider Examples (uncomment and configure as needed):

# Railway PostgreSQL:
# DATABASE_URL=postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway

# Heroku PostgreSQL:
# DATABASE_URL=postgres://username:password@hostname:port/database_name

# Render PostgreSQL:
# DATABASE_URL=postgres://username:password@hostname:port/database_name

# Vercel with Neon:
# DATABASE_URL=postgresql://username:password@hostname:port/database_name?sslmode=require

# AWS RDS:
# DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/quizmaster_prod
